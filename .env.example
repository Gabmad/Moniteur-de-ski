# Set this in Vercel → Project → Settings → Environment Variables
ADMIN_PASSWORD=change-me

# Required on Vercel so /admin can save dates (Storage → Blob → connect to project)
# Locally, dates save to data/availability.json without this token.
# BLOB_READ_WRITE_TOKEN=
